---
"@mcp-gen-ui/schema": minor
"@mcp-gen-ui/core": minor
"@mcp-gen-ui/mcp-server": minor
---

Add application deadline support to benefit records and expose the `getUpcomingDeadlines` MCP tool for retrieving deadline-bearing recommendations sorted by soonest deadline.
