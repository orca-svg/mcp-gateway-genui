---
"@mcp-gen-ui/schema": minor
"@mcp-gen-ui/core": minor
---

Add persona-weighted recommendation scoring contracts and behavior, including `score` and `scoreBreakdown` on benefit summaries, `persona` on user profiles, per-request scoring `weights`, and `householdTypes` on benefit records.
