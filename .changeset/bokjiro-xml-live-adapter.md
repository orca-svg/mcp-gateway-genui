---
"@mcp-gen-ui/adapters": patch
---

Fix the Bokjiro live adapter to request the XML-only `NationalWelfarelistV001` endpoint with required `callTp`/`srchKeyCode` parameters and parse XML responses into validated benefit records.
