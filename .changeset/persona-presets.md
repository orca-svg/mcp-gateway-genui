---
"@mcp-gen-ui/schema": minor
"@mcp-gen-ui/core": minor
"@mcp-gen-ui/mcp-server": minor
---

Replace the provisional recommendation persona ids with the built-in persona registry (`youth_jobseeker`, `university_student`, `newlywed_family`, `single_parent`, `senior`, `general`), expose `resolveWeights` and `listPersonas`, and make request-level score weights merge on top of persona presets.
